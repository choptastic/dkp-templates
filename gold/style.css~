 
/* This is the body, pretty straightforward */
body,.body{	font-family: Arial,sans-serif;
		font-size: 9pt;
		margin:0,0,0,0;
		background-color:#1d1610;
		color:#ddaa00;
	}	

	#pagetitle{
		font-weight:bold;
		font-size:12pt;
		color:#d9ba36;
		text-align:center;
	}

	#logotable{
		position:absolute;
		top:0px;
		left:0px;
		width:100%;
		z-index:1;
	}

	#rightmenu{
		position:absolute;
		top:24px;
		right:0px;
		width:214px;
		z-index:2;
	}
	#statistics{
		z-index:20;
		position:absolute;
		top:0px;
		right:350px;
		height:230px;
		width:350px;
		background-repeat:repeat-x;

	}
	#statisticshidden{
		z-index:20;
		position:absolute;
		top:0px;
		right:350px;
		height:24px;
		width:350px;
		background-repeat:repeat-x;

	}
	#stattable{
	height:100%;
	width:350px;
background-image:url(http://dkpfiles.com/dkp-templates/gold/river.jpg);
}
#stattablehidden{
	height:100%;
	width:350px;
	table-layout:fixed;
}

	#content1{
		z-index:10;
		padding-top:10px;
	}
	#content2{
		text-align:center;
		font-size:9pt;}
	#content3{
		text-align:center;
		font-size:12pt;
		font-weight:bold;}

	.logohoriz{
		height:24px;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/horizbar.gif);
		background-repeat:repeat-x;
	}

	.horizthin{
		height:12px;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/horizthin.gif);
		background-repeat:repeat-x;
	}

	.logohorizflipped{
		height:24px;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/horizflipped.gif);
		background-repeat:repeat-x;
	}

	.logovert{
		width:7px;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/vertbar.gif);
		background-repeat:repeat-y;
	}

.statbotleft{
		width:7px;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/statbotleft.gif);
		background-repeat:repeat-y;
	}

.statbotright{
		width:7px;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/statbotright.gif);
		background-repeat:repeat-y;
	}
			
	.logoremainder{
		background-image:url(http://dkpfiles.com/dkp-templates/gold/tree.jpg);
		background-repeat:repeat-x;
	}

	.menutitle{
		background-image:url(http://dkpfiles.com/dkp-templates/gold/plaque.gif);
		height:30px;
		color:#ffffff;
      		text-align:center;
		font-weight:bold;
		font-size:10pt;
	}

	.menuback{
		background-color:#060606;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/mountain.jpg);
		width:200px;
		background-repeat:no-repeat;
		color:#ddaa00;
	}

	.menucontent{
		color:#ddaa00;
		      font-size:10pt;
		      padding-left:4px;
		      padding-right:4px;
	}

	.menuhoriz{
		background-image:url(http://dkpfiles.com/dkp-templates/gold/horizbar.gif);
		background-repeat:repeat-x;
		height:24px;
	}

	.pagetitle{
		background-image:url(http://dkpfiles.com/dkp-templates/gold/redtile.jpg);
		background-repeat:repeat;
		color:#ffffff;
	}

td.statcontent{
background-image:url(http://dkpfiles.com/dkp-templates/gold/river.jpg);
width:350px;
background-position:bottom center;
}

.statheader{
font-weight:bold;
font-size:10pt;
color:#ffffff;
}

tr.statcontent{
font-size:10pt;
}

.stathoriz{
background-image:url(http://dkpfiles.com/dkp-templates/gold/horizbar.gif);
	background-repeat:repeat-x;
	height:24px;
	width:103px;}

.statlabel{
background-image:url(http://dkpfiles.com/dkp-templates/gold/smallplaque.gif);
	background-repeat:no-repeat;
	height:24px;
	width:130px;
	text-align:center;
	font-size:8pt;
	font-weight:bold;
	background-position:center center;}
	
/* used when [quote] tags are used in posts */
.quote{
	font-size:7.5pt;
background-image:url(http://dkpfiles.com/dkp-templates/gold/river.jpg);
	color:#ffffff;}

.quoteheader {
	font-size:9pt;
background-image:url(http://dkpfiles.com/dkp-templates/gold/redtile.jpg);
	color:#ffffff;}

/* Most tables use this type    */
.list{	border-style:solid;
	border-color:#444400;
	border-width: 1px;}


/* used for the newsletter table */
table.newsletter{
		border: inset;
		border-width: 1px;
		font-family:monospace;
		font-size: 9pt;}

/* Used in the default base as the Title table.  Even with custom layouts, it's used in the Add/Edit Post and other popups that have a title */
Table.Subject{
		border: outset;
		border-width: 1px;}

/* This is used with [title] tags in guides and posts */
.GuideTitle{	font-weight:bold;
		font-size: 14pt;
		color: white;}

		
/* used with [subtitle] tags in guides and posts */
.GuideSubTitle{	font-weight:bold;
		font-size: 11pt;
		color:white;}

/* The Row of the "Title Table" */
.Subject{	font-size: 11pt;
		font-weight: bold;
		background: #111111;
		color: #ffffff}
		
/* This is the "PAge Description" row of the "Title Table" */
.SubSubject{	font-size: 9pt;
		font-weight: bold;
		background: #191919;
		color: #ffffff;}

/* in the default layout, this is the style associated with the Count Down: "x minutes until the next raid" */
.CountDown{	font-size: 16pt;
		font-weight: normal;}
		
/* In the default, is the style of the Next Event Name at the top */
.EventName{	font-size:11pt;
		font-weight:bold; }

/* same as above, only for the Date/Time */
.EventTime{	font-size:11pt;
		font-weight:normal; }
		
/* for the TopStats, is the color of the Statistic itself */
.StatCount{	color: #d1b842;}

/* for the TopStats, is the color of a Statistic designed to get the user's attention */
.StatAlert{	color: #ffff00;}

/* In the default layout, is the style of the "Top Stats" */
.TopStats{	font-size: 7.5pt}

/* In the Default Layout, Is essentially the background information for the Next Event. */
.Event{		background: #AAAAAA;
		font-size:9pt;}
		
/* Error box description */
table.error{	border-width: 1px;
		border-style:outset;}

/* Error box description */
td.error,.missing,div.error{	
		font-size: 9pt;
		background: #771111;
		color: #FFFFFF;}

/* Error box title */
.errorlistlabel{background:#BB1111;
		font-weight:bold;
		color:#FFFFFF;
		font-size:11pt}

/* used on almost every page, is the style of one of the row-types in a table with alternating row types */
.list1,.l1{	font-size: 9pt;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/list1.gif);
		color:#ffffff;
}

/* used on almost every page, is the style of the other of the row-types in a table with alternating row types */
.list2,.l2{	font-size: 9pt;
		background-image:url(http://dkpfiles.com/dkp-templates/gold/list2.gif);
		color:#ffffff;}

/* used commonly, is for rows that want to "Get Your attention" in a table.  IE, Missing Raids on your member profile. */
.listred,.lr{	background: #FF9999;
		color: #000000;
		font-size: 9pt;}

/* used to mark a "new or recently posted" thread. */
.newlist1,.newl1{
		color:#e7ea39;
background-image:url(http://dkpfiles.com/dkp-templates/gold/newlist.gif);
		font-weight:bold;
		font-size: 9pt;}

.newlist2,.newl2{
		color:#e7ea39;
background-image:url(http://dkpfiles.com/dkp-templates/gold/newlist.gif);
		font-weight:bold;
		font-size: 9pt;}

/* used in the copyright notice */
.copyright{font-size: 7.5pt;font-style:italic;color:#ffffff;}

/* used in a few places to just define a normal place to put simple kinda-small text */
.plain{font-size:9pt}

/* to define small text in random places */
.xsmall{font-size:7.5pt}

.calcurrent{
	font-size: 7.5pt;
	color: ;
	border-style:solid;
	border-width:1px;
	border-color:grey;
}


/* The menu header (Menu, Resources, etc) as well as the Calendar Header, and the current date on the calendar */
.MenuCat,.calheader{
		font-size:10pt;
		color: #FFFFFF;}

/* The cell record on the calendar for if there is something scheduled this day */
.calsomething{}

/* The header bar of tables that act as lists */
.listlabel{
		font-size: 9pt;
		font-weight: bold;
		background-image: url(http://dkpfiles.com/dkp-templates/gold/tree.jpg);
		color: #FFFFFF;}

/* The Menu option contents, as well as the body for the calendar */
.MenuChoice,.calbody{	font-size: 9pt;
		color:#ffffff ;}


		
/* While rather rarely used, this is the "color picker" popup. Probably shouldn't ever change */
.cp{		width:10;height:10}

/*  On the forum, this is the little blurb at the top that gives statistic info "Total Posts" "Total Threads" "Deepest Thread" */
.ThreadHeader{	font-size: 11pt;}
	
/* The style associated with the "Add a Post to this thread" link on the forum */
.MakeAPost{	font-weight: bold;
		font-size: 11pt;}

/* The style associated with the pane in the layout containing the body of each post */
.PostText{	font-size: 9pt;
		color:#e9d218;
background-image:url(http://dkpfiles.com/dkp-templates/gold/lighttile.jpg);
		padding:10px;
		border-style:solid;
		border-width:1px;
		border-color: #444400;
		}

/* is the information about the poster on the forum.  The left-most pane on each post */
.PostInfo{	font-size: 9pt;
		color:#bf8b2e;
background-image:url(http://dkpfiles.com/dkp-templates/gold/redtile.jpg);
		padding:5px;
		border-style:solid;
		border-width:1px;
		border-color: #444400;

}

/* used on the [pre] tags in posts and stuff */
pre{		font-size:11pt;}


/* Menu Link Definition */
.listlabel A:visited 	{color: #b6b622;}
.listlabel A:link 	{color: #b6b622;}
.listlabel A:active	{color: #00ff00;}
.listlabel A:hover	{color: #00ff00;}


.menucontent A:visited{text-decoration:none;}
.menucontent A:link{text-decoration:none;}
.menucontent A:active{text-decoration:underline}
.menucontent A:hover{text-decoration:underline}


/* Basic Link Definition */
A:visited{color: #b16523}
A:link	{color: #b16523}
A:active{color: #d19f29}
A:hover	{color: #d19f29}

/* for any list rows that must show up to get the user's attention, this is the link for them */
A.inred:visited	{color:#000000;}
A.inred:link	{color:#000000;}
A.inred:active	{color:#444444;}
A.inred:hover	{color:#444444;}

/* used in on most pages as the buttom class */
input.button{	background: #3e1233;
		color: #ffffff;}

/* used in the default layout as any list items */
li{	
		list-style-image:url(http://dkpfiles.com/<!--System:SD-->/list);
}


/* Any day on the calendar.  Primarily for defining the width of the calendar, as the calendar width SHOULD be (width of .calday) * 7  */
.calday{
	width:20px}

/* This is the Shoutbox style */
.shoutbox{
	overflow:auto;
	height: 200px;
	font-weight:normal;
	font-size: 7.5pt;
}

/* This is the style for the Vent/TS status */
.ventstatus{
	width:100%;
	height:300px;
	overflow:auto;
}


/* The following are the rules for tabs */
div.tabrow{float:left;width:100%;padding-top:5px;}
ul.tabrow, ul.tabrow li{list-style-type:none;margin:0;padding:0}
ul.tabrow{margin-left:10px;}
ul.tabrow li{list-style-image:none;float:left;margin-right: 3px;text-align:center;font-size:inherit}
ul.tabrow a{float:left;width:8em;padding: 5px 0;background-color:#280000;}
ul.tabrow a:hover {background:#333333;}
ul.tabrow li.selected a,ul.tabrow li.selected a:hover {color:#ffffff;background-color:#3a2d01}


/* The next rules (those with "bigcal" in their name) are for the "Big Calendar" */

/* this is the table that displays the calendar info */
.bigcal {width:100%;border-style:inset;border-width:1px;}

/* This is the header of the calendar, displaying the month and year, and the links to the next months */
.bigcalheader{font-size:16pt;font-weight:bold;background-image:url(http://dkpfiles.com/dkp-templates/gold/redtile.jpg)}

/* Day Labels "Sunday" "Monday" etc */
.bigcaldayheader{font-size:10pt;font-weight:bold;background-image:url(http://dkpfiles.com/dkp-templates/gold/redtile.jpg)}

/* This is the class for each day */
.bigcalday {width:14%;border-style:solid;border-color:#777777;border-width:1px;height:100px;background-image:url(http://dkpfiles.com/dkp-templates/gold/river.jpg);background-position:-60px -50px}

/* This is a class that is displayed on event labels (8:00pm - Gruul's Lair) */
.bigcallabel{font-size:8pt;cursor:pointer}

/* This is the div containing the day of the month */
.bigcaldaylabel{font-size:8pt;background-image:url(http://dkpfiles.com/dkp-templates/gold/list1.gif);color:white;text-align:center}

/* The class when a day on the calendar is being hovered over */
.bigcalday_hover{border-color:#ffffff;background-image:url(http://dkpfiles.com/dkp-templates/gold/mountain.jpg) !important;background-position:-60px 0px !important}

/* One of the classes displayed on today */
.bigcalcurrent{border-color:#b6b622;border-width:2px;background-image:url(http://dkpfiles.com/dkp-templates/gold/tree.jpg) !important;background-position: -400px -50px !important}

/* One of the classes to display is something is scheduled on that day */
.bigcalsomething{}

/* This is going to be the Arrows to the right and left */
.bigcalheader a{}

/* This is the div that gets clicked to add a new item.  The default image is a plus.  The Overflow:hidden is because IE, for whatever reason, wants to make it higher than the height attribute */
.bigcalplus{	position:relative;
		top:2px;
		float:right;
		width:10px;
		height:10px;
		overflow:hidden;
		line-height:1px;
		background-image:url(plus.gif);
		cursor:pointer;}		

/* A day that's not in the current month (at the beginning and end of the calendar display) */
.bigcalblank {background-image:url(http://dkpfiles.com/dkp-templates/gold/redtile.jpg)}
